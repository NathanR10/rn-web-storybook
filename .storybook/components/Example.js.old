import React from 'react'
import { View, Text, StyleSheet } from 'react-native'

const Example = (props) => {
  return (
    <View>
      <Text style={{ backgroundColor: props.color }}>Label: {props.label}</Text>
    </View>
  )
}

const styles = StyleSheet.create({

})

export default Example

